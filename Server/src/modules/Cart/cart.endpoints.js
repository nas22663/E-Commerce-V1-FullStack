import { systemRoles } from "../../utils/system-roles.js";
import { endPointsRoles } from "../../utils/general.endpoints.roles.js";

export const cartEndpoints = {
  ADD_CART: endPointsRoles.GENERAL,
};
